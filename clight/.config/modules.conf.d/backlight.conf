backlight:
{
    # restore_on_exit = true;
    # trans_fixed = 800;         # disabled: causes DDC flood over HDMI → clightd errors → segfault
    shutter_threshold = 0.10;   # discard captures with covered webcam

    # timeouts in seconds: [ day, night, event ] on AC
    ac_timeouts = [ 600, 2700, 300 ];
};
