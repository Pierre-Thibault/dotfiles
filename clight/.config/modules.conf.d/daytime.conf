daytime:
{
    # Geographic coordinates for sunrise/sunset calculation.
    # Set your actual location or clight will try to retrieve it automatically.
    latitude  = 45.9;
    longitude = -74.2;

    # Duration in seconds of the "event" window around sunrise/sunset
    event_duration = 1800;

    # sunrise_offset = 0;
    # sunset_offset  = 0;
};
