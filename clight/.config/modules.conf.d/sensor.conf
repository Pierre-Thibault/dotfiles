sensor:
{
    # Ambient brightness -> backlight level curves (11 points, 0.0 to 1.0)
    # X: ambient brightness (0=dark to 10=bright), Y: backlight level
    ac_regression_points   = [ 0.0, 0.10, 0.20, 0.30, 0.40, 0.50, 0.58, 0.65, 0.70, 0.74, 0.78 ];
    batt_regression_points = [ 0.0, 0.07, 0.14, 0.22, 0.30, 0.38, 0.45, 0.51, 0.56, 0.60, 0.64 ];

    # Number of captures per measurement on AC / battery
    captures = [ 5, 3 ];

    # devname = "";   # e.g. "video0" or "iio:device0"; leave empty for auto-detect
    # settings = "";  # e.g. "10094849=1,10094850=166" for manual camera exposure
};
